{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar Taller",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/taller.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            
        }
    ]
}
